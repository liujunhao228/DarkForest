name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  backend:
    name: Backend CI
    uses: ./.github/workflows/backend.yml

  frontend:
    name: Frontend CI
    uses: ./.github/workflows/frontend.yml

  trust-integration:
    name: Trust Integration E2E
    uses: ./.github/workflows/trust-integration.yml