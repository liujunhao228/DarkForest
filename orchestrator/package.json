{
  "name": "darkforest-orchestrator",
  "private": true,
  "version": "0.1.0",
  "description": "DarkForest 独立应用的 dsh profile 进程编排服务：spawn/list/kill AI 玩家（node --import tsx/esm apps/cli/src/bin.ts --profile darkforest），经 mcpserver trust 通道以 DF_AGENT_SID 身份入局",
  "type": "module",
  "engines": {
    "node": ">=22.8.0"
  },
  "packageManager": "npm@11.11.0",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "node --import tsx --test \"test/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.12.3",
    "tsx": "^4.23.1",
    "typescript": "^5.9.2"
  }
}
