# 前端环境变量示例
# 复制为 .env 后按需修改;仅 VITE_ 前缀变量会暴露给前端代码

# 后端 HTTP API 基址(留空则使用同源相对路径,依赖 Vite 代理或后端静态服务)
VITE_API_URL=http://localhost:8080

# 后端 WebSocket 地址(留空则回退到同源 /ws)
VITE_WS_URL=ws://localhost:8080/ws
